# ============================================================================
# RDT-VoI Configuration - SCENARIO A: HIGH-STAKES
# 场景 A：高风险/近阈值决策 - 凸显 EVI 优势
#
# 核心特征：
#   - 高 DDI (0.20-0.35): 大量像元处于决策临界区
#   - 强不对称代价 (FN:FP = 4:1): 漏检代价远高于误报
#   - 🔥 密集候选池 (60%): 充足选择空间避免同质化
#   - 行动受限 (K_action=10): 维护预算紧张
#
# 预期结果：EVI 在期望损失、ROI、Top-K 命中率上显著领先 MI
# ============================================================================

experiment:
  name: "rdt_voi_A_highstakes"
  seed: 42
  output_dir: "results/"

# ----------------------------------------------------------------------------
# Numerical Settings
# ----------------------------------------------------------------------------
numerics:
  linear_solver_tol: 1.0e-8
  cholesky_nugget: 1.0e-6
  pcg_max_iter: 500
  logdet_method: "cholmod"

# ----------------------------------------------------------------------------
# Spatial Domain (20x20 网格，总 400 像元)
# ----------------------------------------------------------------------------
geometry:
  mode: "grid2d"
  nx: 20
  ny: 20
  h: 5.0

# ----------------------------------------------------------------------------
# Prior Distribution (GMRF/SPDE) - 保持高方差热点
# ----------------------------------------------------------------------------
prior:
  nu: 1.0
  kappa: 0.08
  sigma2: 0.25
  alpha: 0.002
  beta: 1.0e-6
  mu_prior_mean: 2.2
  mu_prior_std: 0.3

  # 热点配置（用于空间异质性）
  beta_base: 0.001   # 🔥 5.0 → 0.001: 大幅降低非热点nugget
  beta_hot: 1.0e-5   # 🔥 1.0e-4 → 1.0e-5: 进一步降低热点nugget

  hotspots:
    - center_m: [25, 75]
      radius_m: 20
    - center_m: [75, 25]
      radius_m: 25
    - center_m: [50, 50]
      radius_m: 15

# ----------------------------------------------------------------------------
# Sensor Pool Configuration - 🔥 密集池（60%）避免同质化
# ----------------------------------------------------------------------------
sensors:
  types:
    - name: smartphone
      noise_std: 0.50
      cost_gbp: 30
      footprint: point

    - name: "basic_point"
      noise_std: 0.12
      cost_gbp: 150
      footprint: "point"

    - name: "laser_profiler"
      noise_std: 0.035
      cost_gbp: 800
      footprint: "point"

    - name: photogrammetry
      noise_std: 0.20
      cost_gbp: 100
      footprint: avg3x3

    - name: "vehicle_avg"
      noise_std: 0.30
      cost_gbp: 100
      footprint: "avg5x5"

    - name: inertial_profiler
      noise_std: 0.12
      cost_gbp: 300
      footprint: avg3x3

  pool_strategy: "grid_subsample"
  pool_fraction: 0.60  # 🔥 密集池：240 个候选位置（400×0.60）
  type_mix: [0.15, 0.20, 0.10, 0.20, 0.15, 0.20]

  use_heterogeneous: true
  cost_zones:
    - center_m: [100, 100]
      radius_m: 50
      cost_multiplier: 1.5
      noise_multiplier: 0.8
      allowed_types: ['inertial_profiler', 'photogrammetry']

# ----------------------------------------------------------------------------
# Decision Model - 🔥 高风险参数
# ----------------------------------------------------------------------------
decision:
  tau_iri: null
  tau_quantile: 0.80  # 🔥 高分位阈值，提升 DDI 但避免 100%

  # 代价矩阵 - 修正注释
  L_FP_gbp: 30000.0      # 5000 → 30000
  L_FN_gbp: 120000.0     # 保持不变
  L_TP_gbp: 800.0
  L_TN_gbp: 0.0
  # p_T = 30000 / (30000 + 120000 - 800) ≈ 0.20 (20%) ✅ 修正注释

  target_ddi: 0.30  # Scenario A: 0.25-0.35 | Scenario B: 0.10-0.15
  K_action: 10      # Scenario A: 10 | Scenario B: null

metrics:
  scale_savings_to_domain: true
  coverage_clip: [0.0, 1.0]

# ----------------------------------------------------------------------------
# Sensor Selection Methods - 🔥 前段预算密集
# ----------------------------------------------------------------------------
selection:
  methods:
    - greedy_evi
    - greedy_mi
    - greedy_aopt
    - maxmin
    - uniform
    - random

  budget_type: "count"
  budgets: [1, 3, 5, 8, 10]  # 前段预算密集

  greedy_mi:
    batch_size: 64
    adaptive_pruning: true
    prune_threshold: 0.10
    use_cost: true
    keep_fraction: null  # 🔥 动态计算 max(4k, 0.25C)
    adaptive_keep: true

  greedy_aopt:
    n_probes: 32
    use_cost: true

  greedy_evi:
    n_y_samples: 0
    use_cost: true
    mi_prescreen: false
    keep_fraction: 0.9  # 🔥 动态计算 max(4k, 0.25C)

    # 🔥 关键修复：禁用fold跳过机制
    every_n_folds: 1
    budgets_subset: []
    max_folds: null
    must_budgets: []

  maxmin:
    use_cost: true

# ----------------------------------------------------------------------------
# Expected Value of Information - 🔥 足够样本确保稳定
# ----------------------------------------------------------------------------
evi:
  compute_for: ["greedy_mi", "greedy_evi"]
  method: "monte_carlo"
  monte_carlo_samples: 32  # Scenario A: 32 | Scenario B: 16

# ----------------------------------------------------------------------------
# Cross-Validation
# ----------------------------------------------------------------------------
cv:
  scheme: "spatial_block"
  k_folds: 5
  buffer_width_multiplier: 1.5
  block_strategy: "kmeans"
  ensure_connected: true
  morans_permutations: 999

# ----------------------------------------------------------------------------
# Uncertainty Quantification
# ----------------------------------------------------------------------------
uq:
  bootstrap_method: "spatial_block"
  bootstrap_samples: 1000
  confidence_level: 0.95
  coverage_percentile: 90
  compute_crps: true

# ----------------------------------------------------------------------------
# Diagnostics
# ----------------------------------------------------------------------------
diagnostics:
  morans_i:
    compute: true
    permutations: 999
    alpha: 0.05

  calibration:
    pit_histogram: true
    coverage_curves: true
    reliability_diagram: true

# ----------------------------------------------------------------------------
# 🔥 Visualization - 突出 EVI 的业务价值优势
# ----------------------------------------------------------------------------
plots:
  save_formats: [png, pdf]
  dpi: 300
  style: "seaborn-v0_8-paper"

  budget_curves:
    metrics: ["rmse", "expected_loss_gbp", "coverage_90"]
    show_confidence: true
    confidence_level: 0.95

  performance_profile:
    tau_values: [1.0, 1.05, 1.1, 1.2, 1.5, 2.0, 3.0]
    use_budget_fold_instances: true

  critical_difference:
    alpha: 0.05
    test: "nemenyi"

  business_metrics:
    enable: true
    baseline_method: "uniform"
    show_savings: true
    show_roi: true
    show_cost_efficiency: true

  effect_size:
    enable: true
    plot_heatmaps: true
    plot_pairwise_table: true
    top_k_comparisons: 20

  critical_region:
    enable: true   # Scenario A: true | Scenario B: false
    epsilon: 0.2
    plot_heatmap: true

  expert_plots:
    enable_all: true

    marginal_efficiency:
      enable: true
      normalize_by_cost: true
      unit: "gbp_per_test_point"
      split_by_unit: true

    type_composition:
      enable: true
      show_cost_breakdown: true

    mi_voi_correlation:
      enable: true
      methods: ["greedy_mi", "greedy_evi"]
      correlation_mode_disable_prescreen: false

    calibration_plots:
      enable: true

    spatial_diagnostics:
      enable: true
      show_morans_i: true

    sensor_placement_map:
      enable: true
      budgets_to_show: [5, 10, 15, 20]

    roi_curves:
      enable: true
      baseline_method: "uniform"
      show_near_threshold: true  # 🔥 Scenario A 特有

    robustness_heatmap:
      enable: false  # Scenario A: false | Scenario B: true

    ddi_overlay:
      enable: true   # Scenario A: true | Scenario B: false
      methods_to_compare: ['greedy_evi', 'greedy_mi']

# ----------------------------------------------------------------------------
# Acceptance Criteria
# ----------------------------------------------------------------------------
acceptance:
  m1_grid_size: 100
  m1_budgets: [5, 10, 20]
  m1_check_monotonic: true
  m1_check_diminishing: true

  m2_min_improvement_vs_random: 0.15
  m2_confidence_level: 0.95

  m3_small_instance_n: 25
  m3_small_instance_k: 5
  m3_max_suboptimality: 0.10

  m4_morans_alpha: 0.05
  m4_coverage_tolerance: 0.10
  m4_msse_tolerance: 0.25

# ============================================================================
# 🔥 分层实验设计（专家建议）
# ============================================================================
# 为了更好地展示EVI优势，建议进行以下分层实验：
#
# 1. 阈值敏感性实验：
#    tau_quantile ∈ {0.65, 0.75, 0.88}
#    观察EVI vs MI差距如何随决策阈值变化
#
# 2. 候选池密度实验：
#    pool_fraction ∈ {0.1, 0.4, 0.6}
#    验证"池越密，优化效果越明显"的假设
#
# 3. 成本归一化消融：
#    use_cost ∈ {true, false}
#    评估成本敏感性对算法排序的影响
#
# 建议先用此配置跑基线结果，再进行参数扫描
# ============================================================================